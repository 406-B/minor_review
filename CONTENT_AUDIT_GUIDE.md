# 内容审核系统使用指南

## 概述

内容审核系统基于 DeepSeek AI 模型，对用户提交的内容进行自动审核。审核通过的内容才会发布，未通过的内容会被拒绝并给出审核原因。

## 审核范围

系统会自动审核以下用户输入内容：

1. **帖子内容** (`Post.content`) - 用户发布的帖子
2. **评论内容** (`Comment.content`) - 用户发布的评论
3. **菜品评价** (`Review.content`) - 用户对菜品的文字评价
4. **用户标签** (`Dish.pending_tags`) - 用户为菜品添加的标签

## 审核流程

### 1. 内容提交

当用户创建内容时：

```javascript
// 创建帖子
POST /api/v1/post/create
{
  "subject": "标题",
  "content": "帖子内容"
}

// 创建评论
POST /api/v1/post/comment/create
{
  "content": "评论内容"
}

// 菜品评价
POST /api/dish/{id}/review
{
  "content": "评价内容"
}

// 添加标签
POST /api/dish/{id}/add-tag
{
  "tag_ids": [1, 2]
}
```

### 2. 自动审核

系统会自动调用 DeepSeek API 进行内容审核：

- **审核标准**：内容是否包含暴力、色情、赌博等违法内容；是否包含辱骂、人身攻击等不文明语言；是否包含广告、垃圾信息等
- **审核时间**：通常在1-3秒内完成
- **失败处理**：如果审核服务不可用，默认通过内容

### 3. 审核结果

#### 审核通过

- 内容状态设为 `approved`
- 用户看到成功提示
- 内容正常发布

#### 审核拒绝

- 内容状态设为 `rejected`
- 返回审核失败信息
- 审核原因限制在15字以内

## API 接口

### 获取待审核内容列表

**接口**: `GET /api/v1/profile/audit/pending`

**权限**: 管理员

**响应**:

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "pending_contents": [
      {
        "id": "1",
        "type": "post",
        "title": "帖子标题",
        "content": "帖子内容",
        "author": "用户名",
        "created_at": "2025-12-15T10:00:00Z",
        "images": []
      }
    ],
    "total": 1
  }
}
```

### 审核内容

**接口**: `POST /api/v1/profile/audit/{content_type}/{content_id}`

**权限**: 管理员

**参数**:

- `content_type`: 内容类型 (`post`/`comment`/`review`/`tag`)
- `content_id`: 内容ID

**请求体**:

```json
{
  "action": "approve", // 或 "reject"
  "reason": "审核不通过原因" // 拒绝时必填
}
```

**响应**:

```json
{
  "code": 200,
  "message": "内容审核通过"
}
```

## 管理后台

管理员可以在 Django 管理后台进行批量审核操作：

### 帖子审核 (`PostAdmin`)

- 显示审核状态和时间
- 支持批量批准/拒绝帖子
- 按审核状态过滤

### 评论审核 (`CommentAdmin`)

- 显示审核状态和时间
- 支持批量批准/拒绝评论
- 按审核状态过滤

### 评价审核 (`ReviewAdmin`)

- 显示审核状态和时间
- 支持批量批准/拒绝评价
- 按审核状态过滤

### 标签审核 (`DishAdmin`)

- 显示待审核标签
- 支持批量批准/拒绝标签

## 配置说明

### DeepSeek API 配置

在 `settings.py` 中配置 API 密钥：

```python
# DeepSeek API 配置（用于内容审核）
DEEPSEEK_API_KEY = "your-deepseek-api-key-here"
```

### 审核提示词

审核服务使用以下提示词进行内容审核：

```
请审核以下用户提交的内容是否符合社区规范。社区规范包括：
1. 不包含暴力、色情、赌博等违法内容
2. 不包含辱骂、人身攻击等不文明语言
3. 不包含广告、垃圾信息等
4. 不包含政治敏感内容
5. 不包含虚假信息、谣言等

请严格审核内容，如果发现任何违规情况，请明确指出违规类型和具体原因。
如果内容合规，请回复"通过"。

内容类型: {content_type}
内容标题: {title}
内容详情: {content}

请以JSON格式回复：
{{"status": "通过"|"不通过", "reason": "原因描述（如果不通过）"}}
```

## 数据库字段

审核功能新增的数据库字段：

### Post 模型

- `status`: 审核状态 (`pending`/`approved`/`rejected`)
- `audit_reason`: 审核不通过原因
- `audited_at`: 审核时间

### Comment 模型

- `status`: 审核状态
- `audit_reason`: 审核不通过原因
- `audited_at`: 审核时间

### Review 模型

- `status`: 审核状态
- `audit_reason`: 审核不通过原因
- `audited_at`: 审核时间

## 审核状态说明

- `pending`: 待审核（新创建的内容）
- `approved`: 审核通过（正常发布）
- `rejected`: 审核拒绝（不发布，显示审核原因）

## 性能考虑

1. **异步审核**: 审核过程是同步的，可能会影响响应时间
2. **缓存机制**: 可以考虑缓存审核结果避免重复审核
3. **批量审核**: 管理后台支持批量操作提高效率
4. **错误处理**: API 调用失败时默认通过，确保用户体验

## 扩展建议

1. **异步审核**: 将审核改为异步任务，避免阻塞用户操作
2. **人工复审**: 对自动审核拒绝的内容提供人工复审机制
3. **用户申诉**: 允许用户对审核结果进行申诉
4. **审核日志**: 记录详细的审核日志用于分析和改进
5. **多级审核**: 对重要内容实施多级审核机制

## 故障排除

### 审核服务不可用

如果 DeepSeek API 不可用，系统会：

1. 记录错误日志
2. 默认通过所有内容
3. 继续正常发布

### 审核超时

如果审核超时超过30秒，系统会：

1. 取消审核请求
2. 默认通过内容
3. 记录超时事件

### 审核结果异常

如果 AI 返回的审核结果格式错误，系统会：

1. 记录错误信息
2. 默认通过内容
3. 继续正常流程

## 监控和统计

审核系统的监控指标：

- 审核通过率
- 审核耗时
- 各类型内容违规率
- API 调用成功率

---

**最后更新**: 2024年12月
**版本**: v1.0
**维护者**: 开发团队
